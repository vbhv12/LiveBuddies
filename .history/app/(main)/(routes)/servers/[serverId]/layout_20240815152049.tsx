const ServerIdLayout = ({children, params} : {children: React.ReactNode}) =>{

    const profiel = 
    return (
        <div>
            {children}
        </div>
    )
}

export default ServerIdLayout;